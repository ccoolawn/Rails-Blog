<div class="row">
	<div class="col s10 offset-1">
		<% flash.each do |name, msg| %>
			<div class='chip alert-<%= "#{name}" %>'>
				<%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
			</div>
		<% end %>
	</div>
</div>